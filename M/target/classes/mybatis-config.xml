<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--    Mybatis核心配置文件中的标签必须要按照指定的顺序配置：
               properties?,settings?,typeAliases?,typeHandlers?,objectFactory?,
               objectWrapperFactory?,reflectorFactory?,plugins?,environments?,databaseIdProvider?,mappers?
-->

    <!--    引入properties文件， 此后就可以在当前文件中使用的方式访问value, -->
    <properties resource="jdbc.properties"/>

    <!--这个标签是用来设置类型别名的， 只要在Mybatis范围内就能访问到
    type 中填写之前的正确的名字， alias 设置别名-->
<!--    <typeAliases>-->
<!--        <typeAlias type="" alias=""></typeAlias>-->
        <!--        package 标签表示在这个包中所有的类都具有默认的别名， 就是这个类的类名，且不区分大小写-->
<!--        <package name="com.pan.pojo"/>-->
<!--    </typeAliases>-->

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

   <mappers>
       <package name="com.P.mapper"/>
   </mappers>
</configuration>